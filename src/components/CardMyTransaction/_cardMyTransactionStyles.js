import { makeStyles } from '@material-ui/core';

export const cardMyTransactionStyles = makeStyles((theme) => ({
  root: {
    width: '125px',
  },
  borderBtn: {
    borderColor: 'transparent !important',
  },
}));
